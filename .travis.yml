language: c
os: osx
osx_image: xcode8.2
script:
    - git clone --depth 1 https://github.com/muzuiget/mirror-lua.git lua
    - gcc -O2 -bundle -undefined dynamic_lookup -I./lua/src -o remotedebug.so remotedebug.c
    - ls
